# 硬编码 JSON 引用修改计划

## 问题分析

代码中存在错误的 JSON 引用，需要将其替换为直接的硬编码值：

### 1. 错误引用（需要修改）

| 行号 | 当前代码 | 错误原因 | 建议修改 |
|------|----------|----------|----------|
| 111 | `JSON_text['頭']` | 指向数组 `["spine.006", "face"]`，但代码期望字符串 | `'頭'` |
| 112 | `JSON_text['頭']` | 同上 | `'頭'` |
| 114 | `JSON_text['頭']` | 同上 | `'頭'` |

### 2. 正确引用（保留）

| 行号 | 当前代码 | 状态 | 建议 |
|------|----------|------|------|
| 475 | `JSON_text['添加约束']` | 指向正确的对象 | 保留 |

## 修改方案

### 方案 1：只修改错误引用（推荐）

**修改内容：**
- 将第 111、112、114 行的 `JSON_text['頭']` 替换为 `'頭'`
- 保留第 475 行的 `JSON_text['添加约束']`，因为它是正确的

**优点：**
- 最小化改动
- 保持功能完整性
- 避免引入新问题

### 方案 2：完全硬编码（可选）

**修改内容：**
- 替换所有 JSON 引用为硬编码
- 移除 JSON 文件读取逻辑

**缺点：**
- 大幅增加代码复杂度
- 失去配置灵活性
- 难以维护

## 执行步骤

1. **定位错误引用**：确认第 111、112、114 行的错误引用
2. **替换硬编码值**：将 `JSON_text['頭']` 替换为 `'頭'`
3. **验证修改**：检查代码逻辑是否正确
4. **测试功能**：确保骨骼选择和姿态操作正常

## 预期结果

- ✅ 修复 `JSON_text['頭']` 引用错误
- ✅ 保持 `JSON_text['添加约束']` 功能正常
- ✅ 代码能够正确选择和操作骨骼
- ✅ 不再依赖错误的 JSON 数据结构